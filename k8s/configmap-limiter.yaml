apiVersion: v1
kind: ConfigMap
metadata:
  name: rateforge-limiter-config
  labels:
    app: rateforge-limiter
data:
  NODE_ENV: 'production'
  PORT: '3001'
  LOG_LEVEL: 'info'
  # In Kubernetes, we use the internal service name for Redis
  REDIS_URL: 'redis://rateforge-redis:6379'
