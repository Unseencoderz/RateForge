apiVersion: v1
kind: Service
metadata:
  name: rateforge-dashboard
spec:
  type: LoadBalancer
  selector:
    app: rateforge-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
