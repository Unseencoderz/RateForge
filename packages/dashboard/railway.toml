[setup]
nixPkgs = ["nodejs_22", "pnpm-9_x"]

[build]
builder = "nixpacks"
buildCommand = "pnpm --filter @rateforge/dashboard build"

[deploy]
startCommand = "npx serve -s dist -l $PORT"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
